# Composer
composer.phar
vendor/

# User-uploaded listing photos. These are real students' images, keyed to their
# netid by filename, and this repository is public — they must never be
# committed. Project-owned imagery (favicon, demo samples) lives in assets/
# instead, which is tracked. public/.htaccess is still tracked.
public/images/

# MacOS .DS_Store
**/.DS_Store

# Repomix
repomix-output.txt

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/
